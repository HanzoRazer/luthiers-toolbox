# Runtime Assumption Inventory — Acoustics/Observational

**Sprint:** Acoustics/Observational  
**Terminal:** 4  
**Date:** 2026-05-18  
**Status:** Non-authoritative inventory artifact

---

## Authority Statement

This inventory documents observed runtime assumptions. It does not:
- Modify runtime contracts
- Remove fallback behaviors
- Enforce explicit contracts
- Resolve ontology inference

Implicit assumptions are recorded for later reconciliation.

---

## Applicability Note

**Applicable:** Partial — acoustics is primarily client-side with limited runtime assumptions.

**Scope:** This inventory documents runtime assumptions in acoustics client code, not backend runtime systems. Terminal 2 (Runtime/CAM) owns backend runtime assumptions.

---

## Inventory Entries

### Default Geometry Assumptions

```
assumption: Geometry-derived acoustic states have predefined assumptions
runtime_location: packages/client/src/utils/acoustics/acousticState.ts:15-21
explicit_or_implicit: explicit
contract_backed: yes (DEFAULT_GEOMETRY_ASSUMPTIONS constant)
ontology_inference_risk: Low — assumptions are documented strings
fallback_behavior: Merged with custom assumptions, filtered for duplicates
source_locations:
  - packages/client/src/utils/acoustics/acousticState.ts:15-21
required_reconciliation: None — well-documented
notes: Assumptions: 'Geometry-derived state only', 'No body volume attached', 'No calibrated acoustic model applied', 'No measured response data attached'
```

### Low Confidence Default

```
assumption: Geometry-derived estimates default to 'low' confidence
runtime_location: packages/client/src/utils/acoustics/acousticState.ts:62
explicit_or_implicit: explicit
contract_backed: yes (AcousticState.confidence required field)
ontology_inference_risk: Low — documented invariant
fallback_behavior: N/A — always set explicitly
source_locations:
  - packages/client/src/utils/acoustics/acousticState.ts:62
  - packages/client/src/types/acoustics.ts:50 ("Most geometry-derived estimates should be 'low'")
required_reconciliation: None — well-documented
notes: Prevents overconfidence in estimates. Key design invariant.
```

### Manual Estimate Assumptions

```
assumption: Manual estimates have predefined assumption set
runtime_location: packages/client/src/utils/acoustics/acousticState.ts:129-133
explicit_or_implicit: explicit
contract_backed: yes (MANUAL_ESTIMATE_ASSUMPTIONS constant)
ontology_inference_risk: Low — assumptions are documented strings
fallback_behavior: Replaces geometry assumptions when manual estimate attached
source_locations:
  - packages/client/src/utils/acoustics/acousticState.ts:129-133
required_reconciliation: None — well-documented
notes: Assumptions: 'Manual estimate attached by user', 'No calibrated acoustic model applied', 'Estimate is not generated by the system'
```

### Stale Geometry Warning

```
assumption: Geometry change triggers warning when manual estimates exist
runtime_location: packages/client/src/utils/acoustics/acousticState.ts:186-189
explicit_or_implicit: explicit
contract_backed: partial (warning string, not schema)
ontology_inference_risk: Low — documented behavior
fallback_behavior: Warning added to warnings array
source_locations:
  - packages/client/src/utils/acoustics/acousticState.ts:186-189
required_reconciliation: None — defensive behavior
notes: Warning: 'Geometry has changed since this manual estimate was attached. Re-check whether the estimate still applies.'
```

### Export Validation Gates

```
assumption: Export requires observationalOnly === true and valid schema
runtime_location: packages/client/src/utils/acoustics/measurementArchive.ts:188
explicit_or_implicit: explicit
contract_backed: yes (validation function)
ontology_inference_risk: Low — structural enforcement
fallback_behavior: Validation failure returns errors array
source_locations:
  - packages/client/src/utils/acoustics/measurementArchive.ts:188
required_reconciliation: None — well-documented
notes: Key invariant for archive integrity.
```

### Provenance Reminder Constant

```
assumption: Snapshot provenance reminder is hardcoded constant
runtime_location: packages/client/src/utils/acoustics/diagnosticSnapshot.ts:23-24
explicit_or_implicit: explicit
contract_backed: yes (PROVENANCE_REMINDER constant)
ontology_inference_risk: Low — documentation string
fallback_behavior: N/A — always populated from constant
source_locations:
  - packages/client/src/utils/acoustics/diagnosticSnapshot.ts:23-24
required_reconciliation: None — documentation only
notes: Value: 'All diagnostic summaries remain observational and non-calibrated. Snapshots document state only and do not validate predictions.'
```

### Schema Version Constants

```
assumption: Schema versions are hardcoded constants, not configurable
runtime_location: Multiple files
explicit_or_implicit: explicit
contract_backed: yes (TypeScript literal types)
ontology_inference_risk: Low — versioning is intentional
fallback_behavior: N/A — compile-time enforcement
source_locations:
  - packages/client/src/utils/acoustics/diagnosticSnapshot.ts:15-16 ('diagnostic-snapshot.v1', 'aperture-diagnostic-snapshot')
  - packages/client/src/types/acoustics/measurementArchive.ts:132 ('measurement-archive.v1')
required_reconciliation: None — versioning discipline
notes: Schema migration requires new version constants and migration code.
```

---

## Summary

| Metric | Count |
|--------|-------|
| Assumptions inventoried | 7 |
| Explicit assumptions | 7 |
| Implicit assumptions | 0 |
| Contract-backed | 6 |
| Partially contract-backed | 1 |
| Ontology inference risks | 0 |

---

## Notes

1. Acoustics client code has remarkably well-documented assumptions. No implicit ontology inference detected.

2. All assumptions are explicit constants with clear documentation.

3. Key invariants (low confidence default, observationalOnly) are enforced structurally.

4. Terminal 2 should inventory backend runtime assumptions. This inventory covers client-side only.
