
# Patch N 1.8 — G2/G3 emission for linkers + feed floors
- Adds `server/adaptive_poly_gcode_router.py` and `server/util/g2_emitter.py`.
- Registers `/cam/adaptive3/offset_spiral.nc` to emit NC with G2/G3 arcs + feed floors.
