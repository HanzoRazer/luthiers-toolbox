[project]
name = "luthiers-toolbox-api"
version = "2.0.0"
requires-python = ">=3.11,<3.14"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
exclude = ["data*", "metrics*", "test_support*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
