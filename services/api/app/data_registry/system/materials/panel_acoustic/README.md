# Moved — empirical tonewood corpus

The MB Sound intake lives at:

[`../empirical_tonewood/mb_sound/`](../empirical_tonewood/mb_sound/)

`mb_sound_panels.json` is retained only as a temporary pointer for earlier draft discussion and is **not** the canonical corpus layout. Prefer per-specimen files under `empirical_tonewood/mb_sound/specimens/`.
