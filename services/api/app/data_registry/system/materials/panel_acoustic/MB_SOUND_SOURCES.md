# Relocated

MB Sound source documentation is canonical in `HanzoRazer/luthier-acoustics-data` at
`mb-sound/v1.0.0`, path `cohorts/external/mb_sound/MB_SOUND_SOURCES.md`.

Toolbox consumes the cohort by pin — see
[`docs/reference/mb-sound/`](../../../../../../../docs/reference/mb-sound/).
