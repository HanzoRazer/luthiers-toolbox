[pytest]
# Pytest configuration for The Production Shop API testing

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Coverage options - targeted safety-critical paths with floor
# Note: fail-under=20 matches actual coverage of safety modules (2026-03-28)
# preflight_gate removed: module never imported during tests
addopts =
    --verbose
    --strict-markers
    --tb=short
    --cov=app.core.safety
    --cov=app.rmos.feasibility
    --cov=app.safety
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-report=json:coverage.json
    --cov-branch
    --cov-fail-under=20

# Markers for categorizing tests
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (API endpoints, database)
    smoke: Smoke tests (critical paths)
    slow: Slow-running tests
    router: Router endpoint tests
    geometry: Geometry operation tests
    adaptive: Adaptive pocketing tests
    bridge: Bridge calculator tests
    helical: Helical ramping tests
    cam: CAM operation tests
    export: Export functionality tests
    allow_missing_request_id: Test intentionally allows responses without X-Request-Id
    ai_context: AI context adapter tests

# Asyncio mode
asyncio_mode = auto

# Logging
log_cli = true
log_cli_level = INFO
log_file = tests/test_log.txt
log_file_level = DEBUG

# Warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
