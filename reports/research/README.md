# Research index snapshots

Institutional semantic infrastructure — not transient CI output.

```bash
python scripts/research/build_research_index.py
```

Produces deterministic `research_index.json` (no timestamps, sorted keys). Commit after changing `docs/research/*.md`.
